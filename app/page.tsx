import { EmptyState } from '@sovereignfs/ui';

export default function PaperTrailIndexPage() {
  return (
    <EmptyState
      heading="No projects yet"
      description="Create a project to start pinning evidence and mapping connections."
    />
  );
}
